# Measured results

Generated by `python -m evaluation.run_eval` at 2026-09-21T18:47:39Z. 
Every figure on this page is rendered from `evaluation/report.json`; nothing here is typed by hand. Re-run the command to regenerate both files.

- Extraction provider: `gemini:gemini-3.1-flash-lite`
- Catalog: 578 entries, 578 unique URIs
- Gates: concept coverage ≥ 0.6, margin δ = 0.08, candidate pool = 40, scope fields = ['message']

## 1. Schema and rule compliance

All 20 supplied SIIS rows through `run_pipeline`.

| Metric | Value |
|---|---|
| Rows evaluated | 20 |
| Rows producing a plan | 20 |
| Rows falling back | 0 |
| Schema-valid (Samsung `schema.py`, unmodified) | 100.0% |
| Rule-compliant (`validate.assert_rules`) | 100.0% |
| URL leaks | 0 |
| Catalog-invalid deeplinks emitted | 0 |

## 2. Accuracy

Gold set: 850 labelled steps over 425 catalog equivalence classes, generated from the catalog by `evaluation/synthetic.py`.
54 classes hold more than one entry (193 of 566 resolvable entries), so gold is an equivalence class and a hit on any member counts.

| Metric | Value |
|---|---|
| Cases | 850 |
| accuracy@1 | 90.1% |
| **Wrong** (resolved outside the gold class) | **0.7%** |
| Abstained (no exact match, degrades to `dummy_positive`) | 9.2% |
| **Precision** (correct / decided) | **99.2%** |

Wrong and abstained are reported separately on purpose. An abstention degrades to `bixby://dummy_positive`, which still opens a Settings screen; a wrong deeplink sends the user somewhere else entirely. Only the second reaches the user as a falsehood.

Plan-level deeplink outcomes over the 20 supplied rows:

| Outcome | Count |
|---|---|
| Actions emitted | 82 |
| Exact catalog deeplinks | 6 |
| `dummy_positive` | 5 |
| Null | 76 |
| `auto` stepGroups | 12 |
| `auto` stepGroups carrying a deeplink | 9 (75.0%) |

Wrong resolutions, up to 8:

| Step | Gold | Got |
|---|---|---|
| `Tap the switch next to Charging Feedback to disable it.` | Disable Charging Feedback (offURL) | Disable Charging (offURL, DL-0058) |
| `Turn off Charging Feedback.` | Disable Charging Feedback (offURL) | Disable Charging (offURL, DL-0058) |
| `Turn off Double tap to turn on screen.` | Disable Double tap to turn on screen (offURL) | Disable Double tap to turn off screen (offURL, DL-0090) |
| `Turn on Double tap to turn on screen.` | Enable Double tap to turn on screen (onURL) | Enable Double tap to turn off screen (onURL, DL-0091) |
| `Tap Relumino outline.` | View Relumino outline (onClickURL) | View Relumino outline shortcut (onClickURL, DL-0071) |
| `Navigate to and open Relumino outline.` | View Relumino outline (onClickURL) | View Relumino outline shortcut (onClickURL, DL-0071) |

## 3. Latency

Wall-clock per row, end to end through `run_pipeline`.

| Metric | Value |
|---|---|
| p50 | 4348.9 ms |
| p95 | 11567.2 ms |
| mean | 5807.5 ms |

## 4. Cost and cache

| Metric | Value |
|---|---|
| Extraction provider | `gemini:gemini-3.1-flash-lite` |
| Total cost, 20 rows | $0.03447 |

Cost is an estimate from published per-token rates, not a billed figure. The offline stub provider costs nothing, so a stub run reports $0.

### Semantic cache

Two tiers in process: L0 is an exact hash of the canonical query, L1 is embedding cosine over every stored key using `sentence-transformers/all-MiniLM-L6-v2`.

**Protocol.** Seed the cache with each row's canonical query plus variations 1-5; probe with variations 6-10, which the cache has never seen. Seeding the cache with all ten variations and then querying with those same ten would measure a dictionary lookup, not generalisation.

| Metric | Value | Target |
|---|---|---|
| Rows seeded | 20 | |
| Held-out paraphrases probed | 50 | |
| **Hit rate** | **82.0%** | >= 80.0% |
| ... served this row's own plan | 68.0% | |
| ... served a sibling row's plan, same article | 14.0% | |
| **Cross-article false positives** | **0.0%** | 0% |
| Misses | 9 | |
| Slot-guard rejections | 6 | |
| L0 / L1 hits | 0 / 41 | |
| LLM calls avoided | 41 of 50 | |

A false positive is reported separately because it is worse than a miss. A miss costs one LLM call. A false positive answers the wrong complaint confidently, in milliseconds, with an internally valid plan that nothing downstream can detect as wrong.

Hits are split three ways because the 20 supplied rows carry only 11 distinct articles — six rows share one. A hit on a sibling row with the SAME article is not a falsehood: the plan served is grounded in the very article the request supplied. It is counted apart from an exact-row hit only because it was extracted for a differently-worded sibling complaint. A cross-article hit is the real defect, and it is what the 0% target refers to.

**Latency**, end to end through `run_pipeline` with a provider that raises if called, so every sample is proof the LLM was skipped.

| Path | p50 | p95 | Target p95 |
|---|---|---|---|
| L0 hit (exact) | 0.2 ms | 0.3 ms | 300.0 ms |
| L1 hit (semantic) | 5.8 ms | 7.6 ms | 300.0 ms |
| All hits | 5.7 ms | 7.5 ms | 300.0 ms |
| Hit straight after a store (live path) | 29.5 ms | 32.8 ms | 300.0 ms |
| Cold miss (full pipeline) | 4378.6 ms | 11567.2 ms | n/a |

The last row is the one to read. Seed-then-probe never writes between reads, so it never pays to bring the L1 matrix up to date; a live service writes on every cold miss. That path used to re-encode the whole store on the next lookup and ran linear in its size -- 76.9 ms at 102 keys, 463.8 ms at 552, 1086.8 ms at 1272. New rows are now appended, which is exact for a corpus-independent embedder, and the cost is flat.

Targets: hit rate MET; p95 MET; zero false positives MET.

**What the slot guard is worth.** With the device/domain guard removed, the same held-out probes give 86.0% hit rate and 0.0% cross-article false positives, against 82.0% and 0.0% with it.

On this corpus the guard therefore costs 4.0 points of hit rate and prevents nothing measurable, because the evidence key already makes a cross-article hit impossible. Read that as 'not exercised here', not as 'useless': the guard is what separates two complaints that share an article but not a device or a domain, and an article covering several symptoms is exactly where it would earn its place. It is kept for the same reason gate [3] is kept in section 5 — the measurement is blind to the case it exists for.

#### 4.1 Similarity threshold sweep

Fitted on 10 rows' held-out paraphrases and reported on the other 10 rows' (seed 20260921). Selection rule, declared before the split: zero false positives first, then maximum hit rate.

| Threshold | Hit rate | Correct | False positives | Guard rejections |
|---|---|---|---|---|
| 0.4 <-selected | 60.0% | 40.0% | 0.0% (0) | 20 |
| 0.45 | 60.0% | 40.0% | 0.0% (0) | 20 |
| 0.5 | 60.0% | 40.0% | 0.0% (0) | 20 |
| 0.55 | 56.0% | 36.0% | 0.0% (0) | 19 |
| 0.6 | 56.0% | 36.0% | 0.0% (0) | 19 |
| 0.65 | 54.0% | 34.0% | 0.0% (0) | 19 |
| 0.7 | 48.0% | 30.0% | 0.0% (0) | 19 |
| 0.75 | 44.0% | 28.0% | 0.0% (0) | 16 |
| 0.8 | 32.0% | 22.0% | 0.0% (0) | 13 |
| 0.85 | 22.0% | 18.0% | 0.0% (0) | 9 |
| 0.9 | 8.0% | 4.0% | 0.0% (0) | 6 |

Held-out rows:

| Threshold | Hit rate | Correct | False positives |
|---|---|---|---|
| 0.4 | 86.0% | 72.0% | 0.0% (0) |
| 0.6 | 82.0% | 68.0% | 0.0% (0) |

Shipped threshold = 0.6; the fit half selected 0.4. The fit half selected 0.4, worth +4.0 points of hit rate on the held-out half. It is NOT adopted. Cross-article false positives are zero at every threshold in the sweep, so the sweep is measuring the evidence key rather than the threshold. What the threshold guards is a within-article mismatch, and 11 articles cannot exercise that. The shipped 0.6 is kept as a deliberate safety margin; this is a judgement, not a number the data forced.

## 5. Gate ablation

Same gold set, gates switched on cumulatively. This is what each gate buys.

| Stage | Gates | accuracy@1 | Wrong | Abstained | Precision |
|---|---|---|---|---|---|
| V0 | no gates | 68.4% | 31.6% | 0.0% | 68.4% |
| V1 | + polarity | 84.1% | 15.9% | 0.0% | 84.1% |
| V2 | + scope | 84.1% | 15.9% | 0.0% | 84.1% |
| V3 | + target concept | 97.5% | 2.2% | 0.2% | 97.8% |
| V4 | + margin (shipped) | 90.1% | 0.7% | 9.2% | 99.2% |

Wrong deeplinks fall from 31.6% to 0.7% and precision rises from 68.4% to 99.2% across the four gates.

Gate [3] reads its scope qualifier from the catalog fields `['message']`. Reading `description` as well rejects candidates whose own message a step legitimately names:

| Gate [3] reads | accuracy@1 | Wrong | Abstained | Precision |
|---|---|---|---|---|
| message | 90.1% | 0.7% | 9.2% | 99.2% |
| message + description | 84.4% | 1.4% | 14.2% | 98.4% |

### 5.1 Margin threshold sweep

`MARGIN_DELTA` fitted on a seeded random half (425 cases, seed 20260921) and reported on the held-out half (425 cases). Selection rule, declared before the split: max accuracy@1 subject to precision >= 98.0%.

Fit half:

| δ | accuracy@1 | Wrong | Abstained | Precision |
|---|---|---|---|---|
| 0.0 | 96.7% | 2.8% | 0.5% | 97.2% |
| 0.01 | 93.6% | 2.1% | 4.2% | 97.8% |
| 0.02 ←selected | 92.5% | 1.6% | 5.9% | 98.2% |
| 0.03 | 91.3% | 1.6% | 7.1% | 98.2% |
| 0.04 | 90.8% | 1.6% | 7.5% | 98.2% |
| 0.06 | 89.4% | 0.9% | 9.6% | 99.0% |
| 0.08 | 88.2% | 0.9% | 10.8% | 98.9% |
| 0.12 | 86.8% | 0.9% | 12.2% | 98.9% |
| 0.16 | 84.7% | 0.2% | 15.1% | 99.7% |

Held-out half, for the fit-selected δ and the shipped δ only:

| δ | accuracy@1 | Wrong | Abstained | Precision |
|---|---|---|---|---|
| 0.02 | 93.6% | 1.2% | 5.2% | 98.8% |
| 0.08 | 92.0% | 0.5% | 7.5% | 99.5% |

Shipped δ = 0.08; fit half selected δ = 0.02. δ = 0.02 is not adopted because it produces 3 more wrong deeplinks on the held-out half (1.2% against 0.5%), and a wrong deeplink is the error this project weights hardest. Keep δ = 0.08.

## 6. Per-row results

| Row | Actions | Exact | Dummy | Null | Schema | Rules | URL leak | Fallback | Latency |
|---|---|---|---|---|---|---|---|---|---|
| row_1 | 6 | 0 | 2 | 6 | PASS | PASS | no | - | 5168.0 ms |
| row_2 | 6 | 0 | 0 | 6 | PASS | PASS | no | - | 4864.6 ms |
| row_3 | 4 | 0 | 0 | 4 | PASS | PASS | no | - | 4206.5 ms |
| row_4 | 6 | 0 | 0 | 6 | PASS | PASS | no | - | 6780.0 ms |
| row_5 | 3 | 1 | 1 | 1 | PASS | PASS | no | - | 4210.4 ms |
| row_7 | 1 | 1 | 0 | 0 | PASS | PASS | no | - | 2334.0 ms |
| row_8 | 1 | 1 | 0 | 0 | PASS | PASS | no | - | 3426.5 ms |
| row_9 | 2 | 0 | 0 | 2 | PASS | PASS | no | - | 8177.8 ms |
| row_10 | 1 | 0 | 0 | 1 | PASS | PASS | no | - | 2063.2 ms |
| row_11 | 3 | 0 | 0 | 3 | PASS | PASS | no | - | 3030.2 ms |
| row_12 | 1 | 0 | 0 | 1 | PASS | PASS | no | - | 2251.7 ms |
| row_13 | 5 | 0 | 0 | 5 | PASS | PASS | no | - | 4319.1 ms |
| row_14 | 3 | 0 | 0 | 3 | PASS | PASS | no | - | 2487.6 ms |
| row_15 | 6 | 0 | 0 | 6 | PASS | PASS | no | - | 4554.3 ms |
| row_16 | 6 | 0 | 0 | 6 | PASS | PASS | no | - | 4378.6 ms |
| row_17 | 4 | 0 | 0 | 4 | PASS | PASS | no | - | 7367.1 ms |
| row_19 | 3 | 0 | 0 | 3 | PASS | PASS | no | - | 20262.0 ms |
| row_20 | 6 | 1 | 1 | 5 | PASS | PASS | no | - | 10771.0 ms |
| row_21 | 10 | 2 | 1 | 9 | PASS | PASS | no | - | 11567.2 ms |
| row_22 | 5 | 0 | 0 | 5 | PASS | PASS | no | - | 3929.4 ms |

## 7. Limitations

- **The gold set is derived, not human-labelled.** It is generated from the catalog's own `message` strings, so it measures whether the resolver can recover the entry a step was phrased from. It does not measure whether a real article's wording would reach the right entry. One consequence is specific enough to name: gate [2] reads polarity after subtracting the candidate's own subject from the step, and in this gold set that subject is always present verbatim, so the subtraction always succeeds. On a real article that paraphrases a setting rather than naming it, the subtraction falls back to reading the whole step, and the gate is correspondingly weaker than these numbers suggest.
- **54 message classes are not separable at all.** `View Notification Settings` covers 21 different screens, distinguishable only by `description`, which gate [3] does not read. Those cases are scored against the whole class because no resolver could do better from the step alone.
- **The ablation cannot measure gate [3].** V1 and V2 are identical (84.1% accuracy@1 for both), and this is a property of the gold set, not of the gate. Every generated step is phrased from its own entry's `message`, so it always carries that entry's qualifier and the gate never rejects the answer; the candidates it does reject were outranked anyway. The gate's value shows on steps NOT derived from catalog wording — the factory-reset trap in `backend/tests/test_resolver.py` is the production case it was built for. Read the V2 row as 'costs nothing here', not as 'does nothing'.
- **The cache is measured on 20 rows' paraphrases, not on production traffic.** The held-out paraphrases come from the same extractor that wrote the seeds, so they are more consistent in register than real users would be. The false-positive number is the one to watch as the corpus grows: more stored plans means more chances to land on the wrong one.
- **Latency excludes network time to the LLM** when run against the offline stub. A live provider run reports real extraction latency in the same table.
- **Cost is an estimate** from published per-token rates, not a billed amount.

