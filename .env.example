# Copy to .env. NEVER commit a real key.
LLM_PROVIDER=gemini
LLM_MODEL=gemini-3.1-flash-lite
GEMINI_API_KEY=

# Offline mode — no key required, used by CI and the test suite:
# LLM_PROVIDER=stub

# Resolver gates (docs/API_CONTRACT.md §5)
PRISM_CONCEPT_COVERAGE_MIN=0.60
PRISM_MARGIN_DELTA=0.08
